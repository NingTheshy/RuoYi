# 开发环境配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ry?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8&rewriteBatchedStatements=true
    username: root
    password: root
  data:
    redis:
      host: localhost
      port: 6379
      password: root
      database: 0

jwt:
  secret: DevSecretKeyForJWTTokenGenerationMustBeLongEnough123456

ruoyi:
  cors:
    allowed-origins: http://localhost:3000,http://localhost:5173
