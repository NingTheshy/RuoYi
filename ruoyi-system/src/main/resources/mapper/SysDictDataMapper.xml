<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysDictDataMapper">

    <resultMap id="SysDictDataResult" type="com.ruoyi.system.domain.entity.SysDictData">
        <id property="dictCode" column="dict_code"/>
        <result property="dictSort" column="dict_sort"/>
        <result property="dictLabel" column="dict_label"/>
        <result property="dictValue" column="dict_value"/>
        <result property="dictType" column="dict_type"/>
        <result property="cssClass" column="css_class"/>
        <result property="listClass" column="list_class"/>
        <result property="isDefault" column="is_default"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <select id="selectDictDataList" parameterType="com.ruoyi.system.domain.entity.SysDictData" resultMap="SysDictDataResult">
        SELECT * FROM sys_dict_data
        <where>
            <if test="dictType != null and dictType != ''">AND dict_type = #{dictType}</if>
            <if test="dictLabel != null and dictLabel != ''">AND dict_label LIKE CONCAT('%', #{dictLabel}, '%')</if>
            <if test="status != null and status != ''">AND status = #{status}</if>
        </where>
        ORDER BY dict_sort ASC, dict_code ASC
    </select>

    <select id="selectDictDataByType" parameterType="String" resultMap="SysDictDataResult">
        SELECT * FROM sys_dict_data WHERE dict_type = #{dictType} AND status = '0' ORDER BY dict_sort ASC, dict_code ASC
    </select>

    <select id="countDictDataByType" parameterType="String" resultType="Long">
        SELECT COUNT(*) FROM sys_dict_data WHERE dict_type = #{dictType}
    </select>

</mapper>