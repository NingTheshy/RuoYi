<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysUserMapper">

    <resultMap type="com.ruoyi.system.domain.entity.SysUser" id="SysUserResult">
        <id property="userId" column="user_id"/>
        <result property="deptId" column="dept_id"/>
        <result property="userName" column="user_name"/>
        <result property="nickName" column="nick_name"/>
        <result property="email" column="email"/>
        <result property="phonenumber" column="phonenumber"/>
        <result property="sex" column="sex"/>
        <result property="avatar" column="avatar"/>
        <result property="password" column="password"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="loginIp" column="login_ip"/>
        <result property="loginDate" column="login_date"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectUserVo">
        select user_id, dept_id, user_name, nick_name, email, phonenumber, sex, avatar, password,
               status, del_flag, login_ip, login_date, create_by, create_time, update_by, update_time, remark
        from sys_user
    </sql>

    <select id="selectUserByUserName" parameterType="String" resultMap="SysUserResult">
        <include refid="selectUserVo"/>
        where user_name = #{userName} and del_flag = '0'
    </select>

    <select id="selectUserById" parameterType="Long" resultMap="SysUserResult">
        <include refid="selectUserVo"/>
        where user_id = #{userId} and del_flag = '0'
    </select>

    <select id="selectUserList" parameterType="com.ruoyi.system.domain.entity.SysUser" resultMap="SysUserResult">
        <include refid="selectUserVo"/>
        <where>
            and del_flag = '0'
            <if test="userName != null and userName != ''">
                and user_name like concat('%', #{userName}, '%')
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
            <if test="deptId != null">
                and dept_id = #{deptId}
            </if>
        </where>
        order by create_time
    </select>

    <insert id="insertUser" parameterType="com.ruoyi.system.domain.entity.SysUser" useGeneratedKeys="true" keyProperty="userId">
        insert into sys_user (
            <if test="deptId != null">dept_id,</if>
            user_name, nick_name, email, phonenumber, sex, avatar, password,
            status, del_flag, create_by, create_time, remark
        ) values (
            <if test="deptId != null">#{deptId},</if>
            #{userName}, #{nickName}, #{email}, #{phonenumber}, #{sex}, #{avatar}, #{password},
            #{status}, #{delFlag}, #{createBy}, sysdate(), #{remark}
        )
    </insert>

    <update id="updateUser" parameterType="com.ruoyi.system.domain.entity.SysUser">
        update sys_user
        <set>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="nickName != null">nick_name = #{nickName},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phonenumber != null">phonenumber = #{phonenumber},</if>
            <if test="sex != null">sex = #{sex},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="password != null">password = #{password},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = sysdate(),
            <if test="remark != null">remark = #{remark},</if>
        </set>
        where user_id = #{userId}
    </update>

    <update id="deleteUserByIds" parameterType="Long">
        update sys_user set del_flag = '2' where user_id in
        <foreach item="userId" collection="array" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </update>

    <update id="updateUserLoginInfo">
        update sys_user
        set login_ip = #{loginIp}, login_date = sysdate()
        where user_id = #{userId}
    </update>

    <select id="selectRoleKeysByUserId" parameterType="Long" resultType="String">
        select r.role_key
        from sys_role r
        inner join sys_user_role ur on r.role_id = ur.role_id
        where ur.user_id = #{userId} and r.status = '0'
    </select>

    <select id="selectPermsByUserId" parameterType="Long" resultType="String">
        select distinct m.perms
        from sys_menu m
        inner join sys_role_menu rm on m.menu_id = rm.menu_id
        inner join sys_user_role ur on rm.role_id = ur.role_id
        where ur.user_id = #{userId} and m.status = '0' and m.perms is not null and m.perms != ''
    </select>

</mapper>
