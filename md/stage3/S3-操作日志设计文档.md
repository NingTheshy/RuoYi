# S3-操作日志设计文档

> 状态：⏳ 待开发
> 对应计划：[功能补齐推进计划](../plan/功能补齐推进计划.md#任务3-1操作日志-sysoperlog)

---

## 一、需求分析

### 1.1 业务背景

操作日志用于记录系统用户的操作行为，便于审计追踪和问题排查。通过操作日志，可以了解谁在什么时间执行了什么操作。

### 1.2 功能需求

| 需求点 | 描述 | 优先级 |
|--------|------|--------|
| 日志列表 | 查询操作日志列表，支持分页和筛选 | P0 |
| 日志详情 | 根据日志ID查询日志详情 | P0 |
| 删除日志 | 删除指定的操作日志 | P0 |
| 清空日志 | 清空所有操作日志 | P0 |

### 1.3 非功能需求

- 通过AOP切面自动记录日志
- 日志记录不应影响业务性能
- 支持异步写入数据库

---

## 二、技术方案

### 2.1 数据库设计

#### sys_oper_log 表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| oper_id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 日志ID |
| title | VARCHAR(50) | DEFAULT '' | 操作模块 |
| business_type | CHAR(1) | DEFAULT '0' | 业务类型（0其他 1新增 2修改 3删除） |
| method | VARCHAR(100) | DEFAULT '' | 方法名称 |
| request_method | VARCHAR(10) | DEFAULT '' | 请求方式 |
| operator_type | CHAR(1) | DEFAULT '0' | 操作类别（0其他 1后台用户 2手机端用户） |
| oper_name | VARCHAR(50) | DEFAULT '' | 操作人员 |
| dept_name | VARCHAR(50) | DEFAULT '' | 部门名称 |
| oper_url | VARCHAR(255) | DEFAULT '' | 请求URL |
| oper_ip | VARCHAR(128) | DEFAULT '' | 主机地址 |
| oper_location | VARCHAR(255) | DEFAULT '' | 操作地点 |
| oper_param | VARCHAR(2000) | DEFAULT '' | 请求参数 |
| json_result | VARCHAR(2000) | DEFAULT '' | 返回参数 |
| status | INT | DEFAULT 0 | 操作状态（0正常 1异常） |
| error_msg | VARCHAR(2000) | DEFAULT '' | 错误信息 |
| oper_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 操作时间 |

### 2.2 接口设计

#### 2.2.1 操作日志列表

```
GET /monitor/operlog/list
```

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| pageNum | Integer | ❌ | 页码，默认 1 |
| pageSize | Integer | ❌ | 每页条数，默认 10 |
| title | String | ❌ | 操作模块，模糊匹配 |
| operName | String | ❌ | 操作人员，模糊匹配 |
| businessType | String | ❌ | 业务类型 |
| status | String | ❌ | 操作状态 |
| beginTime | String | ❌ | 开始时间 |
| endTime | String | ❌ | 结束时间 |

**成功响应**：

```json
{
  "code": 200,
  "data": {
    "total": 100,
    "rows": [
      {
        "operId": 1,
        "title": "用户管理",
        "businessType": "2",
        "method": "com.ruoyi.admin.web.system.SysUserController.update",
        "requestMethod": "PUT",
        "operName": "admin",
        "deptName": "研发部门",
        "operUrl": "/system/user",
        "operIp": "127.0.0.1",
        "status": 0,
        "operTime": "2026-07-17 10:00:00"
      }
    ]
  }
}
```

#### 2.2.2 操作日志详情

```
GET /monitor/operlog/{operId}
```

**成功响应**：同上（单条记录）

#### 2.2.3 删除操作日志

```
DELETE /monitor/operlog/{operIds}
```

**成功响应**：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null
}
```

#### 2.2.4 清空操作日志

```
DELETE /monitor/operlog/clean
```

**成功响应**：同上

### 2.3 权限标识

| 权限标识 | 说明 |
|---------|------|
| monitor:operlog:list | 查询操作日志列表 |
| monitor:operlog:query | 查询操作日志详情 |
| monitor:operlog:remove | 删除操作日志 |
| monitor:operlog:clean | 清空操作日志 |

---

## 三、AOP切面设计

### 3.1 自定义注解 @OperLog

```java
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface OperLog {
    String title() default "";
    String businessType() default "0";
    String operatorType() default "0";
    boolean isSaveRequestData() default true;
    boolean isSaveResponseData() default true;
}
```

### 3.2 AOP切面 OperLogAspect

```
请求进入
    │
    ▼
@Around 环绕通知
    │
    ├─► 获取方法上的 @OperLog 注解
    │
    ├─► 获取请求信息（URL、IP、参数等）
    │
    ├─► 执行目标方法
    │
    ├─► 记录返回结果
    │
    └─► 异步保存到数据库
```

---

## 四、代码结构

```
ruoyi-system/
├── domain/
│   ├── entity/
│   │   └── SysOperLog.java            # 操作日志实体
│   └── dto/
│       └── operlog/
│           ├── SysOperLogQueryReq.java # 查询请求DTO
│           └── SysOperLogResp.java     # 响应DTO
├── mapper/
│   └── SysOperLogMapper.java          # Mapper接口
├── resources/
│   └── mapper/
│       └── SysOperLogMapper.xml       # Mapper XML
├── service/
│   ├── SysOperLogService.java         # Service接口
│   └── impl/
│       └── SysOperLogServiceImpl.java # Service实现
└── convert/
    └── SysOperLogConvert.java         # MapStruct转换

ruoyi-common/
└── core/
    ├── annotation/
    │   └── OperLog.java               # 操作日志注解
    └── aspect/
        └── OperLogAspect.java         # AOP切面

ruoyi-admin/
└── web/
    └── monitor/
        └── SysOperLogController.java  # Controller
```

---

## 五、菜单配置

### 5.1 菜单数据

| 字段 | 值 |
|------|------|
| menu_name | 操作日志 |
| parent_id | 新建监控目录 |
| order_num | 1 |
| path | operlog |
| component | monitor/operlog/index |
| menu_type | C |
| perms | monitor:operlog:list |

### 5.2 按钮数据

| 按钮名称 | 权限标识 |
|---------|---------|
| 日志查询 | monitor:operlog:query |
| 日志删除 | monitor:operlog:remove |
| 清空日志 | monitor:operlog:clean |

---

## 六、数据库脚本

```sql
-- 操作日志表
DROP TABLE IF EXISTS sys_oper_log;
CREATE TABLE sys_oper_log (
    oper_id         BIGINT NOT NULL AUTO_INCREMENT COMMENT '日志ID',
    title           VARCHAR(50) DEFAULT '' COMMENT '操作模块',
    business_type   CHAR(1) DEFAULT '0' COMMENT '业务类型（0其他 1新增 2修改 3删除）',
    method          VARCHAR(100) DEFAULT '' COMMENT '方法名称',
    request_method  VARCHAR(10) DEFAULT '' COMMENT '请求方式',
    operator_type   CHAR(1) DEFAULT '0' COMMENT '操作类别（0其他 1后台用户 2手机端用户）',
    oper_name       VARCHAR(50) DEFAULT '' COMMENT '操作人员',
    dept_name       VARCHAR(50) DEFAULT '' COMMENT '部门名称',
    oper_url        VARCHAR(255) DEFAULT '' COMMENT '请求URL',
    oper_ip         VARCHAR(128) DEFAULT '' COMMENT '主机地址',
    oper_location   VARCHAR(255) DEFAULT '' COMMENT '操作地点',
    oper_param      VARCHAR(2000) DEFAULT '' COMMENT '请求参数',
    json_result     VARCHAR(2000) DEFAULT '' COMMENT '返回参数',
    status          INT DEFAULT 0 COMMENT '操作状态（0正常 1异常）',
    error_msg       VARCHAR(2000) DEFAULT '' COMMENT '错误信息',
    oper_time       DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
    PRIMARY KEY (oper_id)
) ENGINE=InnoDB AUTO_INCREMENT=100000 COMMENT='操作日志表';
```

---

## 七、测试用例

| 测试场景 | 请求 | 预期结果 |
|---------|------|---------|
| 查询操作日志列表 | `GET /monitor/operlog/list` | 返回分页数据 |
| 查询操作日志详情 | `GET /monitor/operlog/1` | 返回日志信息 |
| 删除操作日志 | `DELETE /monitor/operlog/1` | 删除成功 |
| 清空操作日志 | `DELETE /monitor/operlog/clean` | 清空成功 |
| 执行操作后自动记录日志 | 调用带 @OperLog 的接口 | 日志记录成功 |
| 权限不足访问 | 无权限用户访问 | 返回 403 |
