# S5-代码生成器设计文档

> 状态：⏳ 待开发
> 对应计划：[功能补齐推进计划](../plan/功能补齐推进计划.md#任务5-1代码生成器)

---

## 一、需求分析

### 1.1 业务背景

代码生成器用于根据数据库表结构自动生成前后端代码，提高开发效率。支持自定义模板、批量生成等功能。

### 1.2 功能需求

| 需求点 | 描述 | 优先级 |
|--------|------|--------|
| 生成表列表 | 查询可生成的数据库表列表 | P0 |
| 预览代码 | 预览生成的代码 | P0 |
| 生成代码 | 生成代码并下载ZIP | P0 |
| 同步表 | 同步数据库表到生成配置 | P0 |
| 修改配置 | 修改表的生成配置 | P0 |
| 删除配置 | 删除表的生成配置 | P0 |

### 1.3 非功能需求

- 支持自定义代码模板
- 支持多种编程语言和框架
- 生成的代码风格统一

---

## 二、技术方案

### 2.1 数据库设计

#### sys_gen_table 表（代码生成表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| table_id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 表ID |
| table_name | VARCHAR(100) | NOT NULL UNIQUE | 表名称 |
| table_comment | VARCHAR(500) | DEFAULT '' | 表描述 |
| sub_table_name | VARCHAR(100) | DEFAULT '' | 子表名称 |
| sub_table_fk_name | VARCHAR(100) | DEFAULT '' | 子表外键名称 |
| class_name | VARCHAR(100) | DEFAULT '' | 实体类名称 |
| tpl_category | VARCHAR(20) | DEFAULT 'crud' | 生成模板分类 |
| package_name | VARCHAR(100) | DEFAULT '' | 包名称 |
| module_name | VARCHAR(30) | DEFAULT '' | 模块名称 |
| business_name | VARCHAR(30) | DEFAULT '' | 业务名称 |
| function_name | VARCHAR(50) | DEFAULT '' | 功能名称 |
| function_author | VARCHAR(50) | DEFAULT '' | 功能作者 |
| gen_type | CHAR(1) | DEFAULT '0' | 生成方式（0zip包 1自定义路径） |
| gen_path | VARCHAR(200) | DEFAULT '/' | 生成路径 |
| options | TEXT | DEFAULT NULL | 其他生成选项 |
| create_by | VARCHAR(64) | DEFAULT '' | 创建者 |
| create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_by | VARCHAR(64) | DEFAULT '' | 更新者 |
| update_time | DATETIME | NULL ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### sys_gen_table_column 表（代码生成字段表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| column_id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 字段ID |
| table_id | BIGINT | NOT NULL | 表ID |
| column_name | VARCHAR(100) | NOT NULL | 字段名称 |
| column_comment | VARCHAR(500) | DEFAULT '' | 字段描述 |
| column_type | VARCHAR(100) | DEFAULT '' | 字段类型 |
| java_type | VARCHAR(50) | DEFAULT '' | Java类型 |
| java_field | VARCHAR(100) | DEFAULT '' | Java字段名 |
| is_pk | CHAR(1) | DEFAULT 'N' | 是否主键（Y是 N否） |
| is_increment | CHAR(1) | DEFAULT 'N' | 是否自增（Y是 N否） |
| is_required | CHAR(1) | DEFAULT 'N' | 是否必填（Y是 N否） |
| is_insert | CHAR(1) | DEFAULT 'Y' | 是否插入（Y是 N否） |
| is_edit | CHAR(1) | DEFAULT 'Y' | 是否编辑（Y是 N否） |
| is_list | CHAR(1) | DEFAULT 'Y' | 是否列表（Y是 N否） |
| is_query | CHAR(1) | DEFAULT 'N' | 是否查询（Y是 N否） |
| query_type | VARCHAR(20) | DEFAULT 'EQ' | 查询方式 |
| html_type | VARCHAR(20) | DEFAULT 'input' | HTML类型 |
| dict_type | VARCHAR(100) | DEFAULT '' | 字典类型 |
| sort | INT | DEFAULT 0 | 排序号 |

### 2.2 接口设计

#### 2.2.1 生成表列表

```
GET /tool/gen/list
```

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| pageNum | Integer | ❌ | 页码，默认 1 |
| pageSize | Integer | ❌ | 每页条数，默认 10 |
| tableName | String | ❌ | 表名称，模糊匹配 |
| tableComment | String | ❌ | 表描述，模糊匹配 |

**成功响应**：

```json
{
  "code": 200,
  "data": {
    "total": 10,
    "rows": [
      {
        "tableId": 1,
        "tableName": "sys_user",
        "tableComment": "用户信息表",
        "className": "SysUser",
        "moduleName": "system",
        "businessName": "user",
        "createTime": "2026-07-17 10:00:00"
      }
    ]
  }
}
```

#### 2.2.2 预览生成代码

```
GET /tool/gen/preview/{tableId}
```

**成功响应**：

```json
{
  "code": 200,
  "data": {
    "controller": "...",
    "service": "...",
    "mapper": "...",
    "entity": "...",
    "dto": "...",
    "convert": "...",
    "xml": "...",
    "vue": "..."
  }
}
```

#### 2.2.3 生成代码

```
POST /tool/gen
```

**请求体**：

```json
{
  "tableId": 1
}
```

**成功响应**：返回 ZIP 文件流

#### 2.2.4 同步数据库表

```
POST /tool/gen/sync
```

**请求体**：

```json
{
  "tableNames": ["sys_user", "sys_role"]
}
```

**成功响应**：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null
}
```

#### 2.2.5 修改生成配置

```
PUT /tool/gen
```

**请求体**：

```json
{
  "tableId": 1,
  "tableName": "sys_user",
  "tableComment": "用户信息表",
  "className": "SysUser",
  "moduleName": "system",
  "businessName": "user",
  "functionName": "用户管理",
  "functionAuthor": "admin"
}
```

**成功响应**：同上

#### 2.2.6 删除生成配置

```
DELETE /tool/gen/{tableIds}
```

**成功响应**：同上

### 2.3 权限标识

| 权限标识 | 说明 |
|---------|------|
| tool:gen:list | 查询生成表列表 |
| tool:gen:preview | 预览生成代码 |
| tool:gen | 生成代码 |
| tool:gen:sync | 同步数据库表 |
| tool:gen:edit | 修改生成配置 |
| tool:gen:remove | 删除生成配置 |

---

## 三、代码结构

```
ruoyi-system/
├── domain/
│   ├── entity/
│   │   ├── GenTable.java              # 生成表实体
│   │   └── GenTableColumn.java        # 生成字段实体
│   └── dto/
│       └── gen/
│           ├── GenTableQueryReq.java
│           ├── GenTableCreateReq.java
│           ├── GenTableUpdateReq.java
│           ├── GenTableResp.java
│           └── GenSyncReq.java
├── mapper/
│   ├── GenTableMapper.java            # 生成表Mapper
│   └── GenTableColumnMapper.java      # 生成字段Mapper
├── resources/
│   ├── mapper/
│   │   ├── GenTableMapper.xml
│   │   └── GenTableColumnMapper.xml
│   └── template/                      # 代码模板目录
│       ├── controller.java.vm
│       ├── service.java.vm
│       ├── serviceImpl.java.vm
│       ├── mapper.java.vm
│       ├── mapper.xml.vm
│       ├── entity.java.vm
│       ├── dto.java.vm
│       ├── convert.java.vm
│       └── index.vue.vm
├── service/
│   ├── GenTableService.java           # Service接口
│   └── impl/
│       ├── GenTableServiceImpl.java   # Service实现
│       └── GenUtils.java              # 代码生成工具类
└── convert/
    └── GenTableConvert.java           # MapStruct转换

ruoyi-admin/
└── web/
    └── tool/
        └── GenController.java         # Controller
```

---

## 四、代码生成流程

```
选择数据库表
    │
    ▼
同步表结构到 sys_gen_table / sys_gen_table_column
    │
    ▼
配置生成选项（包名、模块名、作者等）
    │
    ▼
读取 Velocity 模板文件
    │
    ▼
将表字段信息填入模板变量
    │
    ▼
生成代码文件
    │
    ▼
打包为 ZIP 文件并下载
```

---

## 五、菜单配置

### 5.1 菜单数据

| 字段 | 值 |
|------|------|
| menu_name | 代码生成 |
| parent_id | 新建工具目录 |
| order_num | 1 |
| path | gen |
| component | tool/gen/index |
| menu_type | C |
| perms | tool:gen:list |

### 5.2 按钮数据

| 按钮名称 | 权限标识 |
|---------|---------|
| 预览代码 | tool:gen:preview |
| 生成代码 | tool:gen |
| 同步表 | tool:gen:sync |
| 修改配置 | tool:gen:edit |
| 删除配置 | tool:gen:remove |

---

## 六、数据库脚本

```sql
-- 代码生成表
DROP TABLE IF EXISTS sys_gen_table;
CREATE TABLE sys_gen_table (
    table_id          BIGINT NOT NULL AUTO_INCREMENT COMMENT '表ID',
    table_name        VARCHAR(100) NOT NULL UNIQUE COMMENT '表名称',
    table_comment     VARCHAR(500) DEFAULT '' COMMENT '表描述',
    sub_table_name    VARCHAR(100) DEFAULT '' COMMENT '子表名称',
    sub_table_fk_name VARCHAR(100) DEFAULT '' COMMENT '子表外键名称',
    class_name        VARCHAR(100) DEFAULT '' COMMENT '实体类名称',
    tpl_category      VARCHAR(20) DEFAULT 'crud' COMMENT '生成模板分类',
    package_name      VARCHAR(100) DEFAULT '' COMMENT '包名称',
    module_name       VARCHAR(30) DEFAULT '' COMMENT '模块名称',
    business_name     VARCHAR(30) DEFAULT '' COMMENT '业务名称',
    function_name     VARCHAR(50) DEFAULT '' COMMENT '功能名称',
    function_author   VARCHAR(50) DEFAULT '' COMMENT '功能作者',
    gen_type          CHAR(1) DEFAULT '0' COMMENT '生成方式（0zip包 1自定义路径）',
    gen_path          VARCHAR(200) DEFAULT '/' COMMENT '生成路径',
    options           TEXT DEFAULT NULL COMMENT '其他生成选项',
    create_by         VARCHAR(64) DEFAULT '' COMMENT '创建者',
    create_time       DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_by         VARCHAR(64) DEFAULT '' COMMENT '更新者',
    update_time       DATETIME DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (table_id)
) ENGINE=InnoDB AUTO_INCREMENT=100 COMMENT='代码生成表';

-- 代码生成字段表
DROP TABLE IF EXISTS sys_gen_table_column;
CREATE TABLE sys_gen_table_column (
    column_id     BIGINT NOT NULL AUTO_INCREMENT COMMENT '字段ID',
    table_id      BIGINT NOT NULL COMMENT '表ID',
    column_name   VARCHAR(100) NOT NULL COMMENT '字段名称',
    column_comment VARCHAR(500) DEFAULT '' COMMENT '字段描述',
    column_type   VARCHAR(100) DEFAULT '' COMMENT '字段类型',
    java_type     VARCHAR(50) DEFAULT '' COMMENT 'Java类型',
    java_field    VARCHAR(100) DEFAULT '' COMMENT 'Java字段名',
    is_pk         CHAR(1) DEFAULT 'N' COMMENT '是否主键（Y是 N否）',
    is_increment  CHAR(1) DEFAULT 'N' COMMENT '是否自增（Y是 N否）',
    is_required   CHAR(1) DEFAULT 'N' COMMENT '是否必填（Y是 N否）',
    is_insert     CHAR(1) DEFAULT 'Y' COMMENT '是否插入（Y是 N否）',
    is_edit       CHAR(1) DEFAULT 'Y' COMMENT '是否编辑（Y是 N否）',
    is_list       CHAR(1) DEFAULT 'Y' COMMENT '是否列表（Y是 N否）',
    is_query      CHAR(1) DEFAULT 'N' COMMENT '是否查询（Y是 N否）',
    query_type    VARCHAR(20) DEFAULT 'EQ' COMMENT '查询方式',
    html_type     VARCHAR(20) DEFAULT 'input' COMMENT 'HTML类型',
    dict_type     VARCHAR(100) DEFAULT '' COMMENT '字典类型',
    sort          INT DEFAULT 0 COMMENT '排序号',
    PRIMARY KEY (column_id)
) ENGINE=InnoDB AUTO_INCREMENT=1000 COMMENT='代码生成字段表';
```

---

## 七、测试用例

| 测试场景 | 请求 | 预期结果 |
|---------|------|---------|
| 查询生成表列表 | `GET /tool/gen/list` | 返回分页数据 |
| 预览生成代码 | `GET /tool/gen/preview/1` | 返回各文件代码 |
| 生成代码 | `POST /tool/gen` | 返回 ZIP 文件 |
| 同步数据库表 | `POST /tool/gen/sync` | 同步成功 |
| 修改生成配置 | `PUT /tool/gen` | 更新成功 |
| 删除生成配置 | `DELETE /tool/gen/1` | 删除成功 |
| 权限不足访问 | 无权限用户访问 | 返回 403 |
