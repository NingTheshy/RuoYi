# S1-字典管理设计文档

> 状态：✅ 已完成
> 对应计划：[功能补齐推进计划](../plan/功能补齐推进计划.md#任务1-2字典管理-sysdict)

---

## 一、需求分析

### 1.1 业务背景

字典管理用于维护系统中经常使用的固定数据，如性别、状态、级别等。这些数据在多个业务模块中被频繁引用，通过字典管理可以统一维护和管理。

### 1.2 功能需求

| 需求点 | 描述 | 优先级 |
|--------|------|--------|
| 字典类型管理 | 字典类型的增删改查 | P0 |
| 字典数据管理 | 字典数据的增删改查 | P0 |
| 按类型查询字典数据 | 根据字典类型获取字典数据列表（前端下拉用） | P0 |

### 1.3 非功能需求

- 字典类型编码必须唯一
- 支持 Redis 缓存字典数据，提高查询性能
- 字典数据支持排序

---

## 二、技术方案

### 2.1 数据库设计

#### sys_dict_type 表（字典类型表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| dict_id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 字典类型ID |
| dict_name | VARCHAR(100) | NOT NULL | 字典名称 |
| dict_type | VARCHAR(100) | NOT NULL, UNIQUE | 字典类型编码 |
| status | CHAR(1) | DEFAULT '0' | 状态（0正常 1停用） |
| create_by | VARCHAR(64) | DEFAULT '' | 创建者 |
| create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_by | VARCHAR(64) | DEFAULT '' | 更新者 |
| update_time | DATETIME | NULL ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| remark | VARCHAR(500) | NULL | 备注 |

#### sys_dict_data 表（字典数据表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| dict_code | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 字典数据ID |
| dict_sort | INT | DEFAULT 0 | 排序号 |
| dict_label | VARCHAR(100) | NOT NULL | 字典标签 |
| dict_value | VARCHAR(100) | NOT NULL | 字典值 |
| dict_type | VARCHAR(100) | NOT NULL | 字典类型编码 |
| css_class | VARCHAR(100) | NULL | CSS样式类 |
| list_class | VARCHAR(100) | NULL | 表格样式类 |
| is_default | CHAR(1) | DEFAULT 'N' | 是否默认（Y是 N否） |
| status | CHAR(1) | DEFAULT '0' | 状态（0正常 1停用） |
| create_by | VARCHAR(64) | DEFAULT '' | 创建者 |
| create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_by | VARCHAR(64) | DEFAULT '' | 更新者 |
| update_time | DATETIME | NULL ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| remark | VARCHAR(500) | NULL | 备注 |

### 2.2 接口设计

#### 2.2.1 字典类型列表

```
GET /system/dict/type/list
```

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| pageNum | Integer | ❌ | 页码，默认 1 |
| pageSize | Integer | ❌ | 每页条数，默认 10 |
| dictName | String | ❌ | 字典名称，模糊匹配 |
| dictType | String | ❌ | 字典类型，模糊匹配 |
| status | String | ❌ | 状态（0正常 1停用） |

**成功响应**：

```json
{
  "code": 200,
  "data": {
    "total": 5,
    "rows": [
      {
        "dictId": 1,
        "dictName": "用户性别",
        "dictType": "sys_user_sex",
        "status": "0",
        "remark": "用户性别字典",
        "createTime": "2026-07-17 10:00:00"
      }
    ]
  }
}
```

#### 2.2.2 新增字典类型

```
POST /system/dict/type
```

**请求体**：

```json
{
  "dictName": "用户状态",
  "dictType": "sys_user_status",
  "status": "0",
  "remark": "用户状态字典"
}
```

**成功响应**：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null
}
```

#### 2.2.3 修改字典类型

```
PUT /system/dict/type
```

**请求体**：

```json
{
  "dictId": 1,
  "dictName": "用户性别",
  "dictType": "sys_user_sex",
  "status": "0",
  "remark": "用户性别字典"
}
```

**成功响应**：同上

#### 2.2.4 删除字典类型

```
DELETE /system/dict/type/{dictIds}
```

**成功响应**：同上

#### 2.2.5 字典数据列表

```
GET /system/dict/data/list
```

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| pageNum | Integer | ❌ | 页码，默认 1 |
| pageSize | Integer | ❌ | 每页条数，默认 10 |
| dictType | String | ❌ | 字典类型编码 |
| dictLabel | String | ❌ | 字典标签，模糊匹配 |
| status | String | ❌ | 状态（0正常 1停用） |

**成功响应**：

```json
{
  "code": 200,
  "data": {
    "total": 3,
    "rows": [
      {
        "dictCode": 1,
        "dictSort": 1,
        "dictLabel": "男",
        "dictValue": "0",
        "dictType": "sys_user_sex",
        "isDefault": "Y",
        "status": "0"
      }
    ]
  }
}
```

#### 2.2.6 新增字典数据

```
POST /system/dict/data
```

**请求体**：

```json
{
  "dictSort": 1,
  "dictLabel": "男",
  "dictValue": "0",
  "dictType": "sys_user_sex",
  "isDefault": "Y",
  "status": "0"
}
```

**成功响应**：同上

#### 2.2.7 修改字典数据

```
PUT /system/dict/data
```

**请求体**：

```json
{
  "dictCode": 1,
  "dictSort": 1,
  "dictLabel": "男",
  "dictValue": "0",
  "dictType": "sys_user_sex",
  "isDefault": "Y",
  "status": "0"
}
```

**成功响应**：同上

#### 2.2.8 删除字典数据

```
DELETE /system/dict/data/{dictCodes}
```

**成功响应**：同上

#### 2.2.9 按类型获取字典数据（前端下拉用）

```
GET /system/dict/data/type/{dictType}
```

**成功响应**：

```json
{
  "code": 200,
  "data": [
    { "dictLabel": "男", "dictValue": "0" },
    { "dictLabel": "女", "dictValue": "1" },
    { "dictLabel": "未知", "dictValue": "2" }
  ]
}
```

### 2.3 权限标识

| 权限标识 | 说明 |
|---------|------|
| system:dict:type:list | 查询字典类型列表 |
| system:dict:type:add | 新增字典类型 |
| system:dict:type:edit | 修改字典类型 |
| system:dict:type:remove | 删除字典类型 |
| system:dict:data:list | 查询字典数据列表 |
| system:dict:data:add | 新增字典数据 |
| system:dict:data:edit | 修改字典数据 |
| system:dict:data:remove | 删除字典数据 |

---

## 三、代码结构

```
ruoyi-system/
├── domain/
│   ├── entity/
│   │   ├── SysDictType.java          # 字典类型实体
│   │   └── SysDictData.java          # 字典数据实体
│   └── dto/
│       ├── dict/
│       │   ├── SysDictTypeQueryReq.java
│       │   ├── SysDictTypeCreateReq.java
│       │   ├── SysDictTypeUpdateReq.java
│       │   ├── SysDictTypeResp.java
│       │   ├── SysDictDataQueryReq.java
│       │   ├── SysDictDataCreateReq.java
│       │   ├── SysDictDataUpdateReq.java
│       │   ├── SysDictDataResp.java
│       │   └── DictDataOptionResp.java  # 下拉选项响应DTO
├── mapper/
│   ├── SysDictTypeMapper.java        # 字典类型Mapper
│   └── SysDictDataMapper.java        # 字典数据Mapper
├── resources/
│   └── mapper/
│       ├── SysDictTypeMapper.xml     # 字典类型Mapper XML
│       └── SysDictDataMapper.xml     # 字典数据Mapper XML
├── service/
│   ├── SysDictTypeService.java       # 字典类型Service接口
│   ├── SysDictDataService.java       # 字典数据Service接口
│   └── impl/
│       ├── SysDictTypeServiceImpl.java
│       └── SysDictDataServiceImpl.java
└── convert/
    ├── SysDictTypeConvert.java       # 字典类型转换
    └── SysDictDataConvert.java       # 字典数据转换

ruoyi-admin/
└── web/
    └── system/
        ├── SysDictTypeController.java  # 字典类型Controller
        └── SysDictDataController.java  # 字典数据Controller
```

---

## 四、缓存策略

### 4.1 Redis Key 设计

| Key | 说明 | 过期时间 |
|-----|------|---------|
| dict:type:{dictType} | 存储指定类型的字典数据列表 | 1小时 |

### 4.2 缓存更新策略

- 新增/修改/删除字典数据时，清除对应类型的缓存
- 查询字典数据时，先从缓存获取，缓存不存在时从数据库查询并写入缓存

---

## 五、菜单配置

### 5.1 字典类型菜单

| 字段 | 值 |
|------|------|
| menu_name | 字典管理 |
| parent_id | 1（系统管理） |
| order_num | 6 |
| path | dict |
| component | system/dict/index |
| menu_type | C |
| perms | system:dict:type:list |

### 5.2 按钮权限

| 按钮名称 | 权限标识 |
|---------|---------|
| 字典类型查询 | system:dict:type:list |
| 字典类型新增 | system:dict:type:add |
| 字典类型修改 | system:dict:type:edit |
| 字典类型删除 | system:dict:type:remove |
| 字典数据查询 | system:dict:data:list |
| 字典数据新增 | system:dict:data:add |
| 字典数据修改 | system:dict:data:edit |
| 字典数据删除 | system:dict:data:remove |

---

## 六、数据库脚本

```sql
-- 字典类型表
DROP TABLE IF EXISTS sys_dict_type;
CREATE TABLE sys_dict_type (
    dict_id     BIGINT NOT NULL AUTO_INCREMENT COMMENT '字典类型ID',
    dict_name   VARCHAR(100) NOT NULL COMMENT '字典名称',
    dict_type   VARCHAR(100) NOT NULL UNIQUE COMMENT '字典类型',
    status      CHAR(1) DEFAULT '0' COMMENT '状态（0正常 1停用）',
    create_by   VARCHAR(64) DEFAULT '' COMMENT '创建者',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_by   VARCHAR(64) DEFAULT '' COMMENT '更新者',
    update_time DATETIME DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    remark      VARCHAR(500) DEFAULT NULL COMMENT '备注',
    PRIMARY KEY (dict_id)
) ENGINE=InnoDB AUTO_INCREMENT=100 COMMENT='字典类型表';

-- 字典数据表
DROP TABLE IF EXISTS sys_dict_data;
CREATE TABLE sys_dict_data (
    dict_code   BIGINT NOT NULL AUTO_INCREMENT COMMENT '字典数据ID',
    dict_sort   INT DEFAULT 0 COMMENT '排序号',
    dict_label  VARCHAR(100) NOT NULL COMMENT '字典标签',
    dict_value  VARCHAR(100) NOT NULL COMMENT '字典值',
    dict_type   VARCHAR(100) NOT NULL COMMENT '字典类型',
    css_class   VARCHAR(100) DEFAULT NULL COMMENT 'CSS样式类',
    list_class  VARCHAR(100) DEFAULT NULL COMMENT '表格样式类',
    is_default  CHAR(1) DEFAULT 'N' COMMENT '是否默认（Y是 N否）',
    status      CHAR(1) DEFAULT '0' COMMENT '状态（0正常 1停用）',
    create_by   VARCHAR(64) DEFAULT '' COMMENT '创建者',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_by   VARCHAR(64) DEFAULT '' COMMENT '更新者',
    update_time DATETIME DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    remark      VARCHAR(500) DEFAULT NULL COMMENT '备注',
    PRIMARY KEY (dict_code)
) ENGINE=InnoDB AUTO_INCREMENT=1000 COMMENT='字典数据表';

-- 初始数据 - 字典类型
INSERT INTO sys_dict_type VALUES (1, '用户性别', 'sys_user_sex', '0', 'admin', NOW(), '', NULL, '用户性别字典');
INSERT INTO sys_dict_type VALUES (2, '用户状态', 'sys_user_status', '0', 'admin', NOW(), '', NULL, '用户状态字典');
INSERT INTO sys_dict_type VALUES (3, '菜单状态', 'sys_show_hide', '0', 'admin', NOW(), '', NULL, '菜单状态字典');

-- 初始数据 - 字典数据
INSERT INTO sys_dict_data VALUES (1, 1, '男', '0', 'sys_user_sex', '', '', 'Y', '0', 'admin', NOW(), '', NULL, '');
INSERT INTO sys_dict_data VALUES (2, 2, '女', '1', 'sys_user_sex', '', '', 'N', '0', 'admin', NOW(), '', NULL, '');
INSERT INTO sys_dict_data VALUES (3, 3, '未知', '2', 'sys_user_sex', '', '', 'N', '0', 'admin', NOW(), '', NULL, '');

INSERT INTO sys_dict_data VALUES (4, 1, '正常', '0', 'sys_user_status', '', '', 'Y', '0', 'admin', NOW(), '', NULL, '');
INSERT INTO sys_dict_data VALUES (5, 2, '停用', '1', 'sys_user_status', '', '', 'N', '0', 'admin', NOW(), '', NULL, '');

INSERT INTO sys_dict_data VALUES (6, 1, '显示', '0', 'sys_show_hide', '', '', 'Y', '0', 'admin', NOW(), '', NULL, '');
INSERT INTO sys_dict_data VALUES (7, 2, '隐藏', '1', 'sys_show_hide', '', '', 'N', '0', 'admin', NOW(), '', NULL, '');
```

---

## 七、测试用例

| 测试场景 | 请求 | 预期结果 |
|---------|------|---------|
| 查询字典类型列表 | `GET /system/dict/type/list` | 返回分页数据 |
| 新增字典类型 | `POST /system/dict/type` | 创建成功 |
| 新增重复类型编码 | `POST /system/dict/type`，dictType重复 | 返回 400 错误 |
| 修改字典类型 | `PUT /system/dict/type` | 更新成功 |
| 删除字典类型 | `DELETE /system/dict/type/3` | 删除成功 |
| 查询字典数据列表 | `GET /system/dict/data/list` | 返回分页数据 |
| 新增字典数据 | `POST /system/dict/data` | 创建成功 |
| 修改字典数据 | `PUT /system/dict/data` | 更新成功 |
| 删除字典数据 | `DELETE /system/dict/data/7` | 删除成功 |
| 按类型获取字典数据 | `GET /system/dict/data/type/sys_user_sex` | 返回下拉选项列表 |
| 权限不足访问 | 无权限用户访问 | 返回 403 |
